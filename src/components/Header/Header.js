import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <div>
            <h1>Where else we could use components?</h1>
        </div>
    );
};

export default Header;